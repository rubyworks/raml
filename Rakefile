#!/usr/bin/env ruby

task :default => :test

desc "run qed demos"
task :test do
  system "qed -Ilib qed/"
end

#desc "generate treetop parser"
#task :treetop do
#  Dir.chdir "lib/raml" do
#    sh "tt -o parser.rb treetop/raml.treetop"
#  end
#end

